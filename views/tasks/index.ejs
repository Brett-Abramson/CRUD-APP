<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <div class="container">
        <h1>Gumby Tracker</h1>
        <nav>
            <ul>
                <li>
                    <a href="/">Home</a>
                </li>
                <li>
                    <a href="/tasks/new">Create a new Task</a>
                </li>
            </ul>
        </nav>
        <main>
            <h2>Tasks</h2>
            <ul>
                <% for(let i = 0; i < tasks.length; i++) { %>
                    <li>
                        <%=tasks[i].name%>
                    </li>
                <% } %>
            </ul>
            <div class="table-container">
                <table class="u-full-width">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Date</th>
                            <th>Details</th>
                            <th>Category</th>
                            <th>Tags</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(let i = 0; i < tasks.length; i++) { %>
                            <tr>
                                <td><a href="/tasks/<%=tasks[i]._id%>"><%=tasks[i].name%></a></td>
                                <td><%=tasks[i].date%></td>
                                <td><%=tasks[i].details%></td>
                                <td><%=tasks[i].category%></td>
                                <td><%=tasks[i].tags%></td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </main>
    </div>
</body>
</html>