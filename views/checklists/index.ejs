<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("../partials/head.ejs") %>
    <title>Checklist</title>
</head>
<body>
    <div class="container">
        <header>
            <nav>
                <%- include("../partials/nav.ejs") %>
            </nav>
            <h1>Checklists</h1>
            <ul>
                <li>
                    <a href="/checklists/new">Create A New Checklist</a>
                </li>
            </ul>
        </header>

        <main>
            <h2>List of Checklists</h2>
            <ul>
                <% for( let i = 0; i < checklists.length; i++) { %>
                    <li>
                        <a href="/checklists/<%=checklists[i]._id%>"><%=checklists[i].name%></a>
                    </li>
                <% } %>
            </ul>

        </main>
    </div>
</body>
</html>